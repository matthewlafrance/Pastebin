for local version, use file system because a database adds no functionality for additional complexity.
need a representation for ID's of pastes, use ID as file name.
take ID from user, deny if not unique. 

features: paste, view, delete, list

paste:
take ID and text, write text to file with filename as ID

view:
take ID, retrieve text stored at that ID

delete:
take ID, delete file with that ID

list:
lists all IDs
